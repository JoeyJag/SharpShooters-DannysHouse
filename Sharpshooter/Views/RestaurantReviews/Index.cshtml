@model IEnumerable<Sharpshooter.Models.RestaurantReview>

@{
    ViewBag.Title = "Admin page";
    Layout = "~/Views/Shared/TempLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New Review", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ReviewOfRestraurant)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Rating)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UserName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ReviewOfRestraurant)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Rating)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RestaurantReviewID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RestaurantReviewID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RestaurantReviewID })
        </td>
    </tr>
}

</table>
